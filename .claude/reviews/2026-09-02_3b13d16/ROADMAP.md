# ROADMAP — `into`（3b13d16）レビュー時点の見送り事項

前回（`.claude/reviews/2026-08-28_c91e955/ROADMAP.md`）の項目は全て継続。ここは今回追加分。

## 優先度 中

| ID | 内容 | 推定コスト | 着手タイミング |
| --- | --- | --- | --- |
| G2-04(b) | leader が `into` 使用 × 合流者ありのとき、合流者へ「cache から読み直す」を配ってコピーを消す（put 縮退時のみコピーへ落とす）。G2-02 の「leader 容量不足の全員伝播」と合わせて「フライトの部分成功」ADR で扱う | 中（ADR + 実装 + 並行テスト） | 並行呼び出しで 2N が実害になった報告が出たら |
| 2(a) の緩和 | 使用中バッファ台帳を buffer 同一性から範囲重なり判定へ緩め、1 本の大バッファの互いに素な部分ビューへの並列読みを許す | 小 | その用途の要望が出たら（非破壊） |

## 優先度 低（テストギャップ・G3 🟡）

| ID | 内容 | 推定コスト |
| --- | --- | --- |
| G3-07 | 記録あり一致ヒット（trusted）+ `into` の直接凍結 | 小 |
| G3-08 | leader `into` 無し × 合流者 `into` 有り（成功系） | 小 |
| G3-09 | `into` + `decode` のキャッシュヒット側 | 小 |
| G3-10 | `into` があるとき `expectedBytes` が確保に使われない | 小 |
| G3-12 | `fetchHfFiles` の spec 毎に別器を渡す正常系 | 小 |
| G3-13 | `into` + `recheck: true` のヒット再ハッシュ | 小 |
| G3-15〜20 | assertion の補強（`cache:false` の非接触 / HF #12 の byteOffset / `assertNotStrictEquals` 等） | 極小 |

## 優先度 低（文書・小粒）

- G1-12: 非ゼロ offset の `into`（大バッファの部分ビュー）が動くことの文書化 + テスト。
- G1-13: キャッシュヒットの `body === null` フォールバックは `into` でも一度 2N（文書化のみ）。
- G4-12: ADR 0009 の実測根拠が下流の非公開記録指しである旨の注記。
- G4-14: JSDoc（日本語）と README（英語）の非対称は既存方針のまま。
